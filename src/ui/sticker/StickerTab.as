package ui.sticker
{
    import ui.SimpleTab;
    
    public class StickerTab extends SimpleTab
    {
        public function StickerTab(title:String, status:int=STATUS_DEFAULT)
        {
            super(title, status);
        }
    }
}